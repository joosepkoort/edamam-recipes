<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="alcohol_switch"
        android:summary="No alcohol used or contained"
        android:title="@string/pref_alcohol_free" />
    <SwitchPreference
        android:defaultValue="true"
        android:key="tree_nut_switch"
        android:summary="No tree nuts or products containing tree nuts"
        android:title="tree-nut-free" />
    <SwitchPreference
        android:defaultValue="true"
        android:key="peanut_switch"
        android:summary="No peanuts or products containing peanuts "
        android:title="peanut-free" />


    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_display_name"
        android:inputType="number"
        android:key="number_of_items"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_number_of_itmes" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
</PreferenceScreen>
